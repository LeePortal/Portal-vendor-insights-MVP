/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // The MVP renders the real Portal wordmark from the public site.
  // Using a plain <img> tag, so no next/image domain config is required.
};

export default nextConfig;
